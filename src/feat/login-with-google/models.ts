import {palette} from '../../styles/theme';
import {ButtonStyles} from '../button';

export const googleButtonPalette: ButtonStyles = {
  defaultColor: '#000000',
  pressedColor: '#000000',
  disabledColor: palette.textSecondary,
};
